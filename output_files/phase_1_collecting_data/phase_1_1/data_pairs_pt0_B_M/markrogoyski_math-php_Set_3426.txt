    /**
     * Return the set as a string
     * Set{a, b, c, ...}
     *
     * @return string
     */

    public function __toString(): string
    {
        if ($this->isEmpty()) {
            return 'Ø';
        }
        return 'Set{' . \implode(', ', \array_keys($this->A)) . '}';
    }
