    /**
     * Constructor
     *
     * @param float[] $αs
     */

    public function __construct(array $αs)
    {
        $n = \count($αs);
        for ($i = 0; $i < $n; $i++) {
            Support::checkLimits(self::PARAMETER_LIMITS, ['α' => $αs[$i]]);
        }
        $this->αs = $αs;
    }
