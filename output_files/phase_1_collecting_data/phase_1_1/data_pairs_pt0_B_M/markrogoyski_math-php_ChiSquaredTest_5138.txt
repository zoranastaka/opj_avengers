    /**
     * @test     mean is k
     */

    public function testMean()
    {
        // Given
        $k = 5;
        $chiSquared = new ChiSquared($k);

        // When
        $mean = $chiSquared->mean();

        // Then
        $this->assertEquals($k, $mean);
    }
