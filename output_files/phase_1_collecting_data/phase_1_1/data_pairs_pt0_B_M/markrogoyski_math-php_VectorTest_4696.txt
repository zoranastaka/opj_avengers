    /**
     * @test toString
     */

    public function testToString()
    {
        // Given
        $A = new Vector([1, 2, 3]);

        // When
        $string = $A->__toString();

        // Then
        $this->assertTrue(\is_string($string));
        $this->assertEquals('[1, 2, 3]', $string);
    }
