    /**
     * Add an integer
     *
     * @param int $int
     *
     * @return Rational
     */

    private function addInt(int $int): Rational
    {
        $w = $this->whole + $int;
        return new Rational($w, $this->numerator, $this->denominator);
    }
