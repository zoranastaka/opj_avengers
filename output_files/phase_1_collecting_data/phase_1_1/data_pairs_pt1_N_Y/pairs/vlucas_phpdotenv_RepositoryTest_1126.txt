    /**
     * Returns the key from keyVal(), without reset.
     *
     * @return string
     */

    private function key()
    {
        $keyVal = $this->keyVal();

        /** @var string */
        return \key($keyVal);
    }
